# My Neovim Config!

Never really liked VS Code... it was alright but if coding was going to be a big part of my life, I'd better find a good and fun IDE.

Just clone the repo to your `~/.config/nvim/` folder. Lazy will handle the rest!

```bash
git clone https://github.com/Natzukashi/nvim --branch stable --single-branch
```
